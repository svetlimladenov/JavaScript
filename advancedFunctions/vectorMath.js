const solution = (() => {
    return {
        add(vec1, vec2) {
            return [vec1[0] + vec1[1], vec2[0] + vec2[1]];
        },
        multiply(vec1, scalar) {

        },
        length(vec1) {

        },
        dot(vec1, vec2) {

        },
        cross(vec1, vec2) {

        },
    };
})();

console.log(solution.add([1, 1], [1, 0]));